<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> | Prompts (<%= prompts.length %>)</title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <header>
      <h1><%= title %> (<%= prompts.length %>)</h1>
    </header>

    <nav>
      <a href="/">Home</a>
      <a href="/prompts">All Prompts</a>
      <a href="/prompts/new">New Prompt</a>
    </nav>

    <div class="container">
      <div class="prompts-section">
        <h2>All Prompts</h2>
        <% if (prompts.length === 0) { %>
        <div class="no-prompts">
          <p>No prompts found. Create your first prompt below!</p>
        </div>
        <% } else { %>
        <div class="prompts-grid">
          <% prompts.forEach((prompt) => { %>
          <div class="prompt-card">
            <div class="prompt-header">
              <span class="from"><%= prompt.from %></span>
              <span class="arrow">→</span>
              <span class="to"><%= prompt.to %></span>
            </div>
            <div class="prompt-message"><%= prompt.message %></div>
            <div class="prompt-date">
              <%= prompt.createdAt ? prompt.createdAt.toLocaleDateString() :
              "N/A" %>
            </div>
            <div class="prompt-actions">
              <a href="/prompts/edit/<%= prompt._id %>" class="edit-btn"
                >Edit</a
              >
              <a
                href="/prompts/delete/<%= prompt._id %>"
                class="delete-btn"
                onclick="return confirm('Are you sure you want to delete this prompt?');"
              >
                Delete
              </a>
            </div>
          </div>
          <% }); %>
        </div>
        <% } %>
      </div>

      <div class="form-section">
        <h2>Add New Prompt</h2>
        <form action="/prompts/new" method="post" class="prompt-form">
          <div class="form-group">
            <input type="text" name="from" placeholder="From" required />
          </div>
          <div class="form-group">
            <input type="text" name="to" placeholder="To" required />
          </div>
          <div class="form-group">
            <input type="text" name="message" placeholder="Message" required />
          </div>
          <button type="submit" class="submit-btn">Create Prompt</button>
        </form>
      </div>
    </div>
  </body>
</html>
